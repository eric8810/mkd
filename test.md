# mkd 阅读器测试文档

这是一个 **超轻量级** Markdown 阅读器，基于 *Rust* 和 **GPUI** 构建，支持 macOS 原生渲染。

## 内联样式

正文支持 **粗体**、*斜体*、***粗斜体***、`行内代码`、~~删除线~~、[链接](https://example.com) 以及中文混排测试。行内代码 `fn main() -> ()` 带背景高亮。

## 列表

### 无序列表

- 苹果
- 香蕉
  - 子项一
  - 子项二
- 樱桃

### 有序列表

1. 第一步：安装
2. 第二步：配置
   1. 嵌套步骤 A
   2. 嵌套步骤 B
3. 第三步：完成

## 代码块

```rust
fn main() {
    let msg = "hello mkd";
    println!("{}", msg);
    // 这是一行很长的注释，用来测试代码块在窄窗口下的水平滚动行为是否正常。
}
```

```python
def greet(name: str) -> str:
    return f"Hello, {name}!"
```

## 引用

> 引用块：少即是多。
>
> 这是第二段引用文字，用于测试多段引用。

## 分隔线

---

## 表格

| 功能 | 状态 | 说明 |
| ---- | ---- | ---- |
| 标题 | ✅ | 1-6 级标题 |
| 列表 | ✅ | 有序 / 无序 / 嵌套 |
| 代码块 | ✅ | 语法标签显示 |
| 表格 | ✅ | 基础渲染 |

## 其他

这是一个包含数学符号的段落：$E = mc^2$，以及 `inline code` 结尾。

结尾段落，验证阅读器的滚动、自动换行与可读性排版是否正常工作。
